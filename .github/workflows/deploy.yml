name: Deploy Website to Websupport

on:
  push:
    branches:
      - main

jobs:
  web-deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    - name: Get latest code
      uses: actions/checkout@v3

    - name: Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        port: 21
        server-dir: valorfortis.cz/web/
        # Websupport niekedy vyžaduje vypnutie kontroly certifikátu, ak je nastavený zdieľaný hosting
        dangerously-allow-unverified-ssl: true
